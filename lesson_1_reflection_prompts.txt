How did viewing a diff between two versions of a file help you see the bug that
was introduced?

    made it much easier to compare any changes made and where changes were made

How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?

    Will allow for a fluid workflow, efficiency to find changes, review what a past version was to see pros/cons
    agaisnt new version.

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like Google
docs does?

    pros, you can choose when a logical point to create a commit would be.
    cons, you might forget to do it.

Why do you think some version control systems, like Git, allow saving multiple
files in one commit, while others, like Google Docs, treat each file separately?

    because they aren't for tasks like coding, which contains many lines of code and often many
    files that are interconnected.

How can you use the commands git log and git diff to view the history of files?

    git log will provide the history of commits(changes) made and each commit has an ID.
    git diff command allows you to compare and changes between 2 commits by their IDs.

How might using version control make you more confident to make changes that
could break something?

    its comforting to know that the old versions are still accessible to compare to at any time.

Now that you have your workspace set up, what do you want to try using Git for?

    -uploading projects to git for review
    -practice making and committing changes with comments
